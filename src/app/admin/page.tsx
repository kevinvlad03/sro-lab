import { ComingSoon } from "@/components/coming-soon";

export default function AdminPage() {
  return (
    <ComingSoon
      title="Admin"
      description="Reorder the queue, change job status, upload finished-print photos, manage users. Coming next."
    />
  );
}
