import { Roadmap } from "@/components/roadmap";

export const metadata = {
  title: "Roadmap — SRO Lab",
  description: "What's shipped, what's coming, and what we're considering for SRO Lab.",
};

export default function RoadmapPage() {
  return <Roadmap />;
}
