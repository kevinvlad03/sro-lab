import { ComingSoon } from "@/components/coming-soon";

export default function LoginPage() {
  return (
    <ComingSoon
      title="Sign in"
      description="Email + password sign-in, restricted to @sms-group.com addresses. Coming next."
    />
  );
}
